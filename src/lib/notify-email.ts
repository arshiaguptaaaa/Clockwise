// The one owner-monitoring inbox for every admin notification this app
// sends (waitlist signups, trip invite joins, etc.) — a single shared
// constant rather than the same literal address hardcoded in each action
// file.
export const ADMIN_NOTIFY_EMAIL = "arshia.gupta2027@mastersunion.org";
